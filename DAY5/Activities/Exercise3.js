const newArr = (data) => {
    a = data.split('')
    const last = a[a.length -1]
    if(last === a) {
        console.log(`the last letter of ${data} is 'a'`)
    }
    // else {
    //     console.log(`the last letter of ${data} is not an 'a' `)
    // }
    
}

newArr('Joshna')